# This file is intentionally empty.
# The application uses only built-in Python modules (tkinter, email, mailbox, etc.).
# Tkinter may require system-level installation on Linux (e.g., apt install python3-tk).
